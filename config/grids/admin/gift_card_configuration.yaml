sylius_grid:
    grids:
        madcoders_sylius_gift_card_admin_gift_card_configuration:
            driver:
                name: doctrine/orm
                options:
                    class: "%madcoders_sylius_gift_card.model.gift_card_configuration.class%"
            fields:
                channel:
                    type: twig
                    label: sylius.ui.channel
                    options:
                        template: "@SyliusAdmin/shared/grid/field/channel.html.twig"
                codePrefix:
                    type: string
                    label: madcoders_sylius_gift_card.ui.code_prefix
                codeLength:
                    type: string
                    label: madcoders_sylius_gift_card.ui.code_length
                validityPeriod:
                    type: string
                    label: madcoders_sylius_gift_card.ui.validity_period
                # A twig field rather than a string one: the stored value is an enum case, and an
                # administrator scanning the list needs "Issued by an administrator only", not
                # "admin_only".
                saleMode:
                    type: twig
                    label: madcoders_sylius_gift_card.ui.sale_mode
                    options:
                        template: "@MadcodersSyliusGiftCardPlugin/admin/gift_card_configuration/grid/field/sale_mode.html.twig"
                enabled:
                    type: twig
                    label: sylius.ui.enabled
                    options:
                        template: "@SyliusAdmin/shared/grid/field/boolean.html.twig"
            filters:
                channel:
                    type: entity
                    label: sylius.ui.channel
                    form_options:
                        class: "%sylius.model.channel.class%"
            actions:
                main:
                    create:
                        type: create
                item:
                    update:
                        type: update
                    delete:
                        type: delete
